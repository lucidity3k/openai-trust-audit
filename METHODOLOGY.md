# Methodology / 監査方法論

最終更新: 2026-08-08 JST  
状態: Bootstrap draft

## 1. 目的と射程

本方法論は、OpenAI製品・有料サービス・Support Case・公式表示・公式法令資料に関する公開監査記録を、第三者が追跡・再検証できる形で作成するための手順を定めます。

対象は、観測可能な表示、返信、Case状態、原資料、取得時点の公式ページ、およびそれらの文書照合です。OpenAIまたは担当者の非公開の意図、心理、内部承認経路、未取得ログは対象事実として補完しません。

## 2. 記述区分

すべての実質的記述は、次のいずれかへ分類します。

| 区分 | 定義 | 記載例 |
|---|---|---|
| `OBSERVATION` | 公開証拠から直接確認できる事実 | 「保存画面にはCase終了表示がある」 |
| `USER_REPORT` | 利用者の報告。公開証拠での確認前 | 「利用者は、返信なしでCaseが終了したと報告した」 |
| `INFERENCE` | 観測から導く説明候補 | 「複数Case間で引継ぎが分断された可能性を検討する」 |
| `EVALUATION` | 監査基準に照らした判断 | 「記録された質問と回答の対応が不足している」 |
| `UNCONFIRMED` | 証拠上確定していない内部事情 | 「終了処理を実行した主体は未確認」 |
| `OFFICIAL_SOURCE` | OpenAI、FTC、政府等の公式資料 | 「2026-01-01発効のTerms of Use」 |

同じ段落で複数区分を混在させる場合は、各文の根拠と区分が判別できるようにします。

## 3. 証拠状態

| 状態 | 条件 |
|---|---|
| `PUBLIC_VERIFIED` | 公開ファイル、引用位置、メタデータ、リンク、redactionを再確認済み |
| `PRIVATE_VERIFIED` | private原資料で確認済みだが、公開前レビュー未完了 |
| `REVIEW_PENDING` | 候補資料または索引があり、原資料との対応・完全性を確認中 |
| `USER_REPORTED` | 利用者報告のみ、または公開証拠との対応付け前 |
| `UNCONFIRMED` | 確認資料がない、または確認不能 |
| `WITHHELD` | 個人情報、秘密情報、権利・安全上の理由で公開しない |

`PRIVATE_VERIFIED`は公開閲覧者が検証できることを意味しません。公開文書でCase上の結論を出す際は、原則として`PUBLIC_VERIFIED`の証拠へリンクします。

## 4. 取込みから公開まで

1. **受領** — 原本の由来、取得方法、取得日時、タイムゾーンを記録する。
2. **原本保全** — 原本を上書きせず、バイト数とSHA-256を記録する。
3. **内容索引** — Case Number、日時、送信者表示、質問、回答、添付、Case状態を抽出する。
4. **機微情報レビュー** — メール、電話、住所、決済、認証、Cookie、セッション、端末識別情報等を確認する。
5. **公開用コピー** — 必要な箇所だけredactionし、原本とは別ファイルにする。
6. **redaction検証** — PDFテキスト抽出、画像オブジェクト、メタデータ等から伏字が復元できないことを確認する。
7. **引用照合** — 引用を原文、ページ、画面、日時と照合する。翻訳は別ファイルにする。
8. **分類** — 観測、利用者報告、推論、評価、未確認、公式資料を分ける。
9. **リンク検証** — Case索引、人物索引、時系列、証拠ファイル間のリンクを確認する。
10. **公開** — Draft PRで差分をレビューし、公開範囲を明記する。

## 5. Caseイベントの最小スキーマ

Case内の各イベントは、可能な範囲で次を持ちます。

| 項目 | 内容 |
|---|---|
| Event ID | Case内で一意の識別子。Case番号の推測には使わない |
| Case Number | 画面または原資料で確認できる番号。確認できなければ「不明」 |
| Datetime | 表示日時とタイムゾーン。表示がなければ補完しない |
| Actor | 表示名、表示上の所属、AI表示、利用者等 |
| Event type | 質問、回答、エスカレーション、訂正要求、終了表示等 |
| Exact text | 必要最小限の原文引用 |
| Source | 公開証拠の相対パス、ページ、画像位置 |
| Classification | 記述区分と証拠状態 |
| Open questions | 回答が確認できない項目、内部事情等 |

## 6. 人物と組織の帰属

「誰が回答したか」と「誰が社内で承認したか」を分けます。Support原資料に氏名と所属が表示されている場合、業務上の表示としてCase、日時、回答、原資料に対応付けられます。原資料にない肩書、権限、雇用関係、意思決定責任は追加しません。

OpenAI、OpenAI OpCo, LLC、OpenAI Foundation、OpenAI Group PBC、OpenAI Support、AI-assisted OpenAI Support、ChatGPTは、同一語として処理しません。公式資料またはCase資料が示す範囲だけで関係を記載します。

経営陣・理事の公開役職は公式ページで取得日ごとに確認します。個別Support Caseへの関与や承認は、対応する証拠がない限り記載しません。

## 7. 直接観測の記述

直接記録で確認できる場合は、確認対象を明記して断定します。

- 回答がない場合: 「公開記録上、この質問への回答はない。」
- Case終了が確認できる場合: 「保存画面上、Caseは終了した。終了理由は同画面に表示されていない。」
- 表示変更が確認できる場合: 「取得Aと取得Bで当該文言が異なる。」

「意図的に無視した」「証拠を消そうとした」等の意図は、直接の裏付けがなければ記載しません。意図を確定できないことを理由に、返信内容、削除案内、Case終了等の観測事実を弱めません。

## 8. 公式ページの保存

公式ページには、少なくとも次を付けます。

- URL
- 取得日時とタイムゾーン
- ページ上の公開日・更新日
- HTTPまたはブラウザ取得方法
- PDFまたはスクリーンショットのSHA-256
- 機械翻訳・地域・言語表示の有無
- 取得失敗、Cloudflare、動的表示等の制約

公式スナップショットは取得時点の表示です。現在も同一内容であるとは自動的に扱いません。

## 9. FTC文書との照合

問題ごとに次の順序で記録します。

1. OpenAI側の公開証拠
2. FTCまたは米国政府の公式文言
3. 一致する要素
4. 一致しない要素
5. 証拠上未確認の要素
6. 文書照合上の評価

FTCの政策文書、法令、Complaint、Final Order、Press Releaseを区別します。Complaintの申立てを裁判所の確定認定として書きません。個別事案について行政機関または裁判所が判断していない段階で、その判断があったとは書きません。

一方、公式文書が明示する要件と公開記録が一致する場合は、一致する文言と要素を具体的に記載します。「問題になり得る」だけに弱めません。

## 10. 訂正と版管理

訂正は、対象記述、旧記述、新記述、理由、根拠、変更日を記録します。誤りを痕跡なく削除しません。原資料は修正せず、分析文書または公開用コピーの新しい版を追加します。

## 11. 公開完了条件

Caseまたはfindingを「公開済み」とする前に、次を満たします。

- 原資料と公開用コピーが分離されている
- SHA-256、取得日時、由来、redaction状態がある
- 個人情報と秘密情報のレビューが完了している
- 引用が原文および位置と一致する
- 直接観測と利用者報告が混同されていない
- 人物の役割とCase関与を過大に帰属していない
- 公式資料の版と取得日が記録されている
- リンクが開き、参照先が存在する

## English summary

The audit uses six distinct statement classes and a separate evidence-status model. Raw evidence is preserved, redacted copies are independently verified, and every public claim must be traceable to a source location. Official sources and case behavior are recorded separately before element-by-element comparison.

